# 轻量级口播数字人系统 (Talking Avatar Platform)

## 技术需求与架构文档 (V1.0)

### 1. 项目概述与业务目标

本项目旨在打造一个轻量级的端到端 AI 数字人合成平台。用户可以通过管理后台上传静态图片与音频素材，并输入播报文本。系统后台将调用 AI 模型（TTS 与面部驱动算法），将文本与素材合成，最终渲染出嘴型同步的“数字人播报”视频。

### 2. 系统架构设计

为了保证系统的稳定性与易维护性，且考虑到 AI 渲染任务极度消耗算力，本系统采用**微服务分离与容器化部署**方案。所有静态资源与视频产物均通过网络传输，解耦计算与存储。

| 模块 | 职责与功能 | 核心技术栈 |
| --- | --- | --- |
| **前端 (Web UI)** | 提供后台管理面板、文件上传、文本配置、任务状态轮询与视频结果预览。 | **React + Vite**UI: **shadcn-admin** (基于 Tailwind CSS) |
| **API 控制面** | 负责请求鉴权、业务逻辑处理、数据库 CRUD 以及将合成任务派发至消息队列。 | **Go**框架: **Gin**ORM: **GORM** |
| **AI 算法节点** | 监听队列，下载素材，执行 TTS 与面部驱动模型推理，生成视频并回传。 | **Python 3.10**队列: Redis Queue / Celery |
| **对象存储层** | 统一管理原始图片、音频素材以及生成的视频文件，解耦服务间的文件依赖。 | **RustFS**(完全兼容 S3 协议) |
| **中间件与数据** | 任务状态缓存、消息排队分发、业务元数据持久化。 | **MySQL 8.0****Redis** |

### 3. 数据流转管线 (Data Pipeline)

本系统的核心难点在于异步长耗时任务的处理，以下为标准的数据流转生命周期：

1. **资源入库 (Upload)**：用户在 `shadcn-admin` 前台上传图片与音频。前端将文件流发给 Go (Gin) API。API 调用 AWS SDK v2，通过 S3 协议将文件直传至 RustFS，并将返回的 `S3 Key` 存入 MySQL 数据库。
2. **排队机制 (Queueing)**：用户在前端提交“播报文本”。Go API 生成一个唯一的任务，将其连同文本及对应的 `S3 Key` 封装为 JSON payload，推送至 Redis 队列。
3. **算力调度 (Worker Fetch)**：部署在后台的 Python AI 节点持续监听 Redis。获取到任务后，调用 `boto3`（配置为 RustFS 的 Endpoint），将图片与音频从对象存储下载到 AI 节点的本地临时目录（如 `/tmp/`）。
4. **模型推理 (Inference)**：AI 节点依次执行：
* **TTS**：将文本与参考音频合成为 `tts_out.wav`。
* **面部驱动**：将图片与 `tts_out.wav` 结合，渲染出包含精确口型的 `final_avatar.mp4`。


5. **产物回传 (Callback)**：AI 节点将 `final_avatar.mp4` 上传回 RustFS。获取到访问 URL 后，更新 MySQL 中该任务的状态为 `Completed`，并保存视频链接。
6. **前台展示 (Display)**：前端在提交任务后持续轮询 Go API。当状态变为 `Completed`，前端直接获取 RustFS 提供的视频 URL，在网页播放器中渲染展示。

### 4. 核心数据字典 (Database Schema)

基于 GORM 的约定，主要涉及以下两张核心表：

**Table: `avatars` (数字人素材库)**

* `id`: Primary Key
* `name`: 形象名称 (String)
* `image_s3_key`: 关联图片的 S3 存储路径 (String)
* `voice_audio_s3_key`: 关联音频的 S3 存储路径 (String, nullable)
* `created_at`: 创建时间

**Table: `broadcast_tasks` (合成任务队列表)**

* `id`: Primary Key
* `avatar_id`: 关联素材 ID (Foreign Key)
* `script_text`: 用户配置的播报长文本 (Text)
* `status`: 任务状态 Enum (`pending`, `processing`, `completed`, `failed`)
* `output_video_s3_url`: 最终视频的可访问 S3 链接 (String, nullable)
* `created_at`: 创建时间
* `updated_at`: 更新时间

### 5. API 接口定义

后端 Go (Gin) 必须提供以下核心接口（遵循 RESTful 规范）：

* **`POST /api/avatars`**
* **功能**：接收多部分表单数据（multipart/form-data），上传图片与音频，代理直传 RustFS。


* **`GET /api/avatars`**
* **功能**：获取已创建的数字人素材列表，供前端选择。


* **`POST /api/tasks`**
* **功能**：提交合成任务。接收 `avatar_id` 与 `script_text`，压入 Redis 队列并返回 Task ID。


* **`GET /api/tasks/:id`**
* **功能**：前端轮询接口，返回任务的当前 `status` 及最终的 `output_video_s3_url`。



### 6. 工程化与部署要求

* **完全的 S3 协议对接**：在代码层面，必须将 RustFS 视作标准的 AWS S3。Go 端使用 `aws-sdk-go-v2`，Python 端使用 `boto3`。不允许使用本地文件路径进行微服务间的文件传递。
* **容器化编排**：整个系统（含 Nginx、Gin 后端、Python AI Worker、MySQL、Redis）通过 `docker-compose.yml` 进行统一编排与一键启动。
* **优雅的 AI Worker 降级**：初期开发阶段，AI Worker 需支持 **Mock（模拟）模式**（如睡眠 10 秒后返回静态视频），以保障前后端接口对接通畅，待联调完毕后再注入真实的 AI 推理代码。