name: talking-avatar-platform

services:
  # ---- Infrastructure: intentionally NOT exposed to the host ----
  mysql:
    image: mariadb:11
    restart: unless-stopped
    environment:
      MARIADB_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root}
      MARIADB_DATABASE: ${MYSQL_DATABASE:-talking_avatar}
      MARIADB_USER: ${MYSQL_USER:-talking}
      MARIADB_PASSWORD: ${MYSQL_PASSWORD:-talking123}
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 5s
      timeout: 5s
      retries: 20

  redis:
    # RedisStack (includes RediSearch FT.*) is required by the private
    # knowledge base (RAG worker). Plain redis:8.2.2-alpine works for the
    # rest of the platform but has no vector-search module.
    image: ${REDIS_IMAGE:-redis/redis-stack-server:latest}
    restart: unless-stopped
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:-}
    command: ["sh", "-c", "exec redis-server --appendonly yes $${REDIS_PASSWORD:+--requirepass $$REDIS_PASSWORD}"]
    # Exposed for the hybrid deployment: the Python AI Worker runs natively on
    # the macOS host (MPS) and must reach Redis via localhost.
    ports:
      - "${REDIS_HOST_PORT:-6379}:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  # Dev stand-in for RustFS (S3-compatible object storage).
  minio:
    image: minio/minio:latest
    restart: unless-stopped
    command: ["server", "/data", "--console-address", ":9001"]
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
    # Exposed for the hybrid deployment: the host-native worker needs the
    # S3-compatible endpoint at localhost:9000.
    ports:
      - "${MINIO_HOST_PORT:-9000}:9000"
    volumes:
      - minio-data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 5s
      timeout: 5s
      retries: 20

  # One-shot bootstrap: create bucket + public read policy (dev only).
  minio-init:
    image: minio/mc:latest
    restart: "no"
    depends_on:
      minio:
        condition: service_healthy
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
      S3_BUCKET: ${S3_BUCKET:-talking-avatar}
    entrypoint: ["/bin/sh", "-c"]
    command:
      - >-
        mc alias set local http://minio:9000 "$$MINIO_ROOT_USER" "$$MINIO_ROOT_PASSWORD" &&
        mc mb --ignore-existing "local/$$S3_BUCKET" &&
        mc anonymous set download "local/$$S3_BUCKET" &&
        echo "object storage bucket ready"

  # ---- Application services (internal network only) ----
  api:
    build:
      context: ./backend
    restart: unless-stopped
    environment:
      API_PORT: "8080"
      GIN_MODE: release
      MYSQL_DSN: "${MYSQL_USER:-talking}:${MYSQL_PASSWORD:-talking123}@tcp(mysql:3306)/${MYSQL_DATABASE:-talking_avatar}?charset=utf8mb4&parseTime=True&loc=Local"
      REDIS_ADDR: redis:6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-}
      REDIS_DB: "0"
      TASK_QUEUE_KEY: ${TASK_QUEUE_KEY:-talking_avatar:tasks}
      LIVE_CONTROL_QUEUE_KEY: ${LIVE_CONTROL_QUEUE_KEY:-talking_avatar:live_control}
      AVATAR_INIT_QUEUE_KEY: ${AVATAR_INIT_QUEUE_KEY:-talking_avatar:avatar_init}
      KNOWLEDGE_INGEST_QUEUE_KEY: ${KNOWLEDGE_INGEST_QUEUE_KEY:-talking_avatar:knowledge_ingest}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_BASE_URL: ${OPENAI_BASE_URL:-https://api.deepseek.com}
      OPENAI_MODEL: ${OPENAI_MODEL:-deepseek-v4-flash}
      EMBED_SERVER_URL: ${EMBED_SERVER_URL:-http://host.docker.internal:8090}
      ADMIN_USERNAME: ${ADMIN_USERNAME:-admin}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-admin123}
      S3_ENDPOINT: http://minio:9000
      S3_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
      S3_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin}
      S3_BUCKET: ${S3_BUCKET:-talking-avatar}
      S3_USE_SSL: "false"
      S3_PUBLIC_BASE_URL: ${S3_PUBLIC_BASE_URL:-http://localhost:8080/media}
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:5173,http://localhost:3000,http://localhost:8080}
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy

  # ---- Docker mock worker: COMMENTED OUT ----
  # The real AI worker runs natively on the host (AI_MODE=real) and shares the
  # same Redis queue; keeping this mock service up makes it race the host
  # worker and produce espeak male-voice/static-image results. Re-enable only
  # for a demo without host models:
  #
  # worker:
  #   build:
  #     context: ./worker
  #   restart: unless-stopped
  #   environment:
  #     REDIS_ADDR: redis:6379
  #     REDIS_PASSWORD: ${REDIS_PASSWORD:-}
  #     REDIS_DB: "0"
  #     TASK_QUEUE_KEY: ${TASK_QUEUE_KEY:-talking_avatar:tasks}
  #     API_BASE_URL: http://api:8080
  #     S3_ENDPOINT: http://minio:9000
  #     S3_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
  #     S3_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin}
  #     S3_BUCKET: ${S3_BUCKET:-talking-avatar}
  #     S3_PUBLIC_BASE_URL: ${S3_PUBLIC_BASE_URL:-http://localhost:8080/media}
  #     AI_MODE: ${AI_MODE:-mock}
  #   depends_on:
  #     api:
  #       condition: service_started
  #     redis:
  #       condition: service_healthy
  #     minio:
  #       condition: service_healthy

  # Real-time streaming gateway (SRS v5). The host-native AI worker pushes
  # RTMP to 1935; HTTP-FLV/API stay on the internal network and are exposed
  # to the browser through the nginx /live proxy (port 8081 is host-only for
  # debugging).
  srs:
    image: ossrs/srs:5
    restart: unless-stopped
    ports:
      - "1935:1935"   # RTMP ingest (host worker -> rtmp://localhost:1935/live/<id>)
      - "1985:1985"   # SRS HTTP API
      - "8081:8080"   # HTTP-FLV / HLS (nginx proxies /live to srs:8081)
    volumes:
      - srs-data:/usr/local/srs/objs

  # ---- Public entry point (the only host-published service) ----
  frontend:
    build:
      context: ./frontend
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8080}:80"
    depends_on:
      - api

  # ---- 观众端（独立 Next.js 客户端，不属于管理后台） ----
  client:
    build:
      context: ./client
    restart: unless-stopped
    ports:
      - "${CLIENT_PORT:-3000}:3000"
    environment:
      # Server-side proxy target: browser only talks to :3000, no CORS needed.
      API_ORIGIN: http://api:8080
      LIVE_ORIGIN: http://srs:8080
    depends_on:
      - api

volumes:
  srs-data:
  mysql-data:
  redis-data:
  minio-data:
