[project]
name = "video-gen-service"
version = "0.1.0"
description = "Video generation microservice: provider registry + Redis job queue (LivePortrait now, ComfyUI later)"
requires-python = ">=3.11,<3.14"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "redis>=5,<7",
]

[tool.uv]
package = false
