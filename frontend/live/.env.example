# Server-side origins read at runtime (route-handler proxy, no CORS needed).
# Local dev: host nginx serves both /api and /live on :8080.
# docker-compose overrides them to the internal api / srs containers.
API_ORIGIN=http://localhost:8080
LIVE_ORIGIN=http://localhost:8080
