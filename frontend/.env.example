# Base URL of the app/API origin when developing locally with Vite.
# Leave empty when served by nginx in docker (same origin, /api proxied).
# When using the dockerized stack, point at the exposed nginx port:
VITE_API_BASE_URL=http://localhost:8080
